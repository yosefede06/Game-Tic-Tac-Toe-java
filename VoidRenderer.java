/**
 * An empty implementation of Renderer that doesn't actually display anything on the screen,
 * but is still a form of rendering
 */
public class VoidRenderer implements Renderer {
    /**
     * Receives a board and doesn't actually display anything.
     * @param board
     */
    @Override
    public void renderBoard(Board board) {

    }
}
